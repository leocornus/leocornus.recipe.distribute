language: python
python:
    - 2.7
    - 3.2
    - 3.3

install:
    - pip install --use-mirrors 'zc.buildout zope.testing zope.testrunner six'
    - python setup.py install

script: python setup.py test
